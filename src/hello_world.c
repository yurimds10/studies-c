#include <stdio.h>          // include information about stardard library

int printHelloWorld() {     // define a function called main that received no arguments values
  printf("Php is dead!\n"); // main calls library function printf to print this sequence of characters
  printf("Hello World\n");  // \n represents the newline character
}

// 10 page of book "C programming language